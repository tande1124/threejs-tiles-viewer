window.__GLTF_GEO_CONFIG__ = {

  // 中央子午线经度（度）。
  // 投影坐标系（如 CGCS2000 3度带）的中心经线，
  // 用于将模型局部坐标对齐到正确的投影带。
  centralMeridianDeg: 114,

  // 投影 X 偏移量（米）。
  // CGCS2000 投影坐标系中的东向坐标（含 500km 假东偏移），
  // 将模型锚点映射到真实地理坐标的 X 分量。
  offsetX: 460019.023,

  // 投影 Y 偏移量（米）。
  // CGCS2000 投影坐标系中的北向坐标，
  // 将模型锚点映射到真实地理坐标的 Y 分量。
  offsetY: 3939980.213,

  // 高程偏移量（米）。
  // 每 +1 → 模型整体抬高 1 米，每 -1 → 降低 1 米。
  // 用于校正模型海拔与实际地形的偏差。
  offsetZ: 0,

  // 垂直缩放比例。
  // 1 = 原始比例不变；>1 拉伸地形起伏；<1 压缩地形起伏。
  // 通常保持为 1，仅在需要夸张地形高差时调整。
  verticalScale: 1,
}
